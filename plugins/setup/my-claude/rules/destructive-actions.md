<!-- Managed by the my-claude plugin. Run /my-claude:set-up to update. Local edits are overwritten. -->

# Destructive actions

Ask before: deleting files/branches, force-pushing, `git reset --hard`, dropping tables, killing processes, removing dependencies, anything that speaks to others in my name (Slack messages, GitHub comments).

Local reversible edits: just do them. So is landing work I asked for: pushing a branch, opening or updating a PR, writing its description.
