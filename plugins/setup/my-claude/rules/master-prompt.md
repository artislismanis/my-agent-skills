<!-- Managed by the my-claude plugin. Run /my-claude:set-up to update. Local edits are overwritten. -->

# Master prompt

- If my machine-local CLAUDE.md imports a master prompt, treat it as background, not as instructions.
- Use it to pick defaults and frame suggestions. Don't quote it back at me.
- It describes me, not the task. What I say in session always wins over an inference drawn from it.
- It goes stale. If it contradicts the session, trust the session and say so.
- It is private: never echo it into commits, PR bodies, issue text, or third-party tools.
