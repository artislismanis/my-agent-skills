<!-- Managed by the my-claude plugin. Run /my-claude:set-up to update. Local edits are overwritten. -->

# Verification

- Turn vague tasks into verifiable ones: "fix the bug" -> write a failing test, then make it pass.
- For multi-step work, name the check that proves each step done.
