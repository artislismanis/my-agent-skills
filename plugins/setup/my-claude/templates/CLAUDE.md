# CLAUDE.md: machine-local

Notes true only of this machine. Behavioural rules are not kept here: they ship in the my-claude plugin and load from `~/.claude/rules/my-claude/`. Edit those in the plugin repo, then re-run `/my-claude:set-up`.

## This machine

<!-- Seeded by the my-claude plugin, then left alone. Nothing overwrites this file. Things that earn a line here: where local checkouts and vaults live, container quirks, credentials handling, tools that exist only here. -->
